# @playwright/test to dependencies or install it with npm install -D @playwright/test
npm install

# start frontend
npm start

# start backend (in separated terminal)
cd server
node server.js

# create folder "tests", write tests in a file called "ui.test.js" and after that start testing the application

npm test

# if everything is working as expected, start to create the CI/CD pipeline
# npx playwright install is needed
npx playwright install

# run tests:
npm test

