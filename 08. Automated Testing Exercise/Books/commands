# install all the dependencies
# keep in mind that chai 5.1.0 is not fully supported so you can add this to the package.json file:

{
  "dependencies": {
    "body-parser": "^1.20.2",
    "chai": "^4.2.0",
    "chai-http": "^4.2.0",
    "express": "^4.18.3",
    "mocha": "^10.3.0"
  }
}

# create tests folder
# navigate to it and create api.test.js
# write the tests in it
# and run it with:
npx mocha api.test.js


